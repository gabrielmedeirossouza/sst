{
  "name": "sst",
  "version": "1.0.0",
  "description": "Smart Subtitle Translator",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --recursive --parallel --stream run dev",
    "client": "pnpm --filter @sst/client-popup",
    "sandbox": "pnpm --filter @sst/sandbox",
    "package:global-message": "pnpm --filter @sst/global-message"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sst/global-message": "workspace:*",
    "vitest": "^0.34.4"
  },
  "devDependencies": {
    "@sst/global-message-protocol": "workspace:*",
    "sass": "^1.66.1"
  }
}
